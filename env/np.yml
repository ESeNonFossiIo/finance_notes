name: my_notes
channels:
  - defaults
dependencies:
  - pip
  - python
  - ffmpeg
  - pip:
      - ffmpeg
      - ipykernel
      - ipython
      - ipywidgets
      - jupyter
      - matplotlib
      - numpy
      - scipy
      - seaborn
      - widgetsnbextension
prefix: my_notes
